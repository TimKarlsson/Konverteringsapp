<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_info"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="tim_tub.konverteringsapp.infoActivity">


    <Button
        android:id="@+id/infoButtonTorque"
        android:text="@string/infoButtonTorque"
        android:textSize="@dimen/knappText"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_above="@+id/infoCenter"
        android:layout_centerHorizontal="true"
        android:onClick="torque"
        />

    <!-- "Space" för att centrera knapparna-->
    <Space
        android:id="@+id/infoCenter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        />

    <Button
        android:text="@string/infoButtonKvalite"
        android:id="@+id/infoButtonQuality"
        android:textSize="@dimen/knappText"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/infoCenter"
        android:onClick="quality"
         />



    <!--<Button
        android:id="@+id/infoButtonTap"
        android:text="Button"
        android:textSize="@dimen/knappText"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/infoButtonQuality"
        android:layout_centerHorizontal="true"
         />-->
</RelativeLayout>
